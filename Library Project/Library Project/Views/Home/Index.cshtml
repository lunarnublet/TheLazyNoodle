@model List<Book>
@{
    ViewBag.Title = "Index";
}

@section heading{
    <div class=header>
        <h1 class=header-heading>Library Database</h1>
    </div>
}

@section body{
    <table>
        <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Author(s)</th>
            <th>Publishing Year</th>
            <th>Genre</th>
            <th>Available Copies</th>
            <th>Total Copies</th>
        </tr>
        @foreach (Book book in Model)
        {
            <tr>
                @Html.Partial("_DisplayBookPartialView", book)
                <td>
                    @Html.ActionLink("Edit", "EditBook", "Book", new { id = book.Id }, null)
                    @Html.ActionLink("Delete", "DeleteBook", "Book", new { id = book.Id }, null)
                    @Html.ActionLink("Check Out", "CheckOut", "Book", new { id = book.Id }, null)
                </td>
            </tr>
        }
    </table>
}

